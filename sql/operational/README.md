# Operational Database Scripts

SQL scripts to create the original 12 operational tables.

## Files
- `create-tables.sql` - All table definitions
- Sample data insertion scripts

## How to Use
1. Create empty database: `CREATE DATABASE dela_services;`
2. Run the SQL scripts in order
3. Verify with: `SELECT COUNT(*) FROM funerals;`
